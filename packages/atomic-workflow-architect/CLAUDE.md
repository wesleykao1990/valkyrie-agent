@AGENTS.md

# Claude Code notes

Use `AGENTS.md` as this module's bootstrap. Preserve the whole-system authority boundaries and do not claim live Atomic behavior without an exercised, pinned runtime contract.
