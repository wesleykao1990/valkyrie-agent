@AGENTS.md

# Claude Code notes

Use the repository instructions in `AGENTS.md` as the controlling project protocol. Prefer small, reviewable commits. Preserve the public HTTP and MCP contracts unless an ADR explicitly changes them.
